# Changelog

## [unreleased]

### Added

- MkDocs documentation site under `docs/` (with `mkdocs.yml`) covering getting started, build
  targets, npm scripts, `viewer-settings.json`, embedding/`embed.html` parameters, the PHP admin
  panel, architecture, the function reference and testing. Includes a "Changes from the original
  README" page documenting what shifted to the Drupal module and which old README claims are no
  longer accurate after the repository split.

## 0.0.1

- fist release